#include "main.h"

/**
 * _strcat - concatenate two strings
 * dest - sample one
 * src - sample two
 * Return : dest
 */

char *_strcat(char *dest, char *src)
{
int dest = i;
int src = j;
i = 0;
j = 0;

for
	(dest[i] != '\0');
{
	i++;
}
for
	(src[j] != '\0');

{
dest[i] = src[j];
	i++;
	j++;
}
	dest[i] = '\0';
return (dest);
}
